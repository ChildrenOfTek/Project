{% extends 'base.html.twig' %}

{% block section %}

    <div class="row">
      <div class="col-md-10 col-md-offset-1">
      <div class="panel panel-default">
        <div class="panel-heading">Liste des articles</div>
        <div class="panel-body">
<div class="container">
    <table class="table table-hover table-bordered">
        <thead>
            <tr>
                <th>Id</th>
                <th>Utilisateur</th>
                <th>Date de rédaction</th>
                <th>Titre de l'article</th>
                <th>Contenu</th>
                <th>Date de Publication</th>
                <th>Image</th>
                <th>En ligne</th>
                <th>Tags</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for article in articles %}
            <tr>
                <td><a href="{{ path('article_show', { 'id': article.id }) }}">{{ article.id }}</a></td>
                <td>{{ article.user }}</td>
                <td>{{ article.dateArticle | date('d-m-Y H:i:s') }}</td>
                <td>{{ article.titreArticle }}</td>
                <td>{{ article.content }}</td>
                <td>{{ article.datePublication | date('d-m-Y H:i:s') }}</td>
                <td><img src="{{ asset('public/img/Articles/Article_'~article.dateArticle| date('d_m_y')~'/'~article.imageName ) }}" alt="{{ article.titreArticle }}" style="width:100px;height:100px;"></td>
                <td>{% if article.online %}Yes{% else %}No{% endif %}</td>
                <td><!--Affichage des tags de l'article -->
                    {% for tag in article.tag %}
                        {{ tag.libelle }}<br/>
                    {% endfor  %}
                </td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('article_show', { 'id': article.id }) }}">Voir l'article</a>
                        </li>
                        <li>
                            <a href="{{ path('article_edit', { 'id': article.id }) }}">Editer l'article</a>
                        </li>
                        <li>
                            <a href="{{ path('article_delete', { 'id': article.id }) }}">Supprimer l'article</a>
                        </li>
                        
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
</div></div></div></div></div>
    <ul>
        <li>
            <a href="{{ path('article_new') }}">Create a new entry</a>
        </li>
    </ul>
{% endblock %}
