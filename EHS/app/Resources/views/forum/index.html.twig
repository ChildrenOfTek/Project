{% extends 'base.html.twig' %}

{% block section %}
    <h1>Liste des forums</h1>

    <table class='table'>
        <thead>
            <tr>

                <th>Nom</th>
                <th>Description</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for forum in forums %}
            <tr>

                <td><a href="{{ path('forum_show',{ 'id': forum.id })}}">{{ forum.libForum }}</td>
                <td>{{ forum.description }}</td>
                <td>
                    <a href="{{ path('forum_edit', { 'id': forum.id }) }}">Modifier</a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <form method="POST" action="{{ path('forum_new') }}">
            <input type="submit" value="Créer un nouveau sujet" >
    </form>

    </ul>
{% endblock %}
