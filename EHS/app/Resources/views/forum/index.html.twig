{% extends 'base.html.twig' %}

{% block section %}

    <div class='panel'>
        <h1>Liste des forums</h1>
        <div class="index-forum table-responsive">
            <table class='table'>
                <thead>
                    
                    <tr>
                        <th>Nom</th>
                        <th>Description</th>
                        {% if(is_granted('ROLE_ADMIN')) %}
                            <th>Actions</th>
                        {% endif %}
                    </tr>

                </thead>
                <tbody>
                    {% for forum in forums %}
                    <tr>

                        <td><a href="{{ path('forum_show',{ 'id': forum.id })}}">{{ forum.libForum }}</td>
                        <td>{{ forum.description }}</td>
                        {% if(is_granted('ROLE_ADMIN')) %}
                            <td><a href="{{ path('forum_edit', { 'id': forum.id }) }}">Modifier</a></td>
                        {% endif %}

                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
        {% if(is_granted('ROLE_ADMIN')) %}
        <form method="POST" action="{{ path('forum_new') }}">
            <input type="submit" value="Créer un nouveau sujet" >
        </form>
        {% endif %}
    </div>

    

    </ul>
{% endblock %}
