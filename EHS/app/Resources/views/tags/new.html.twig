{% extends 'base.html.twig' %}

{% block section %}
    <h1>Création d'un tag</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Créer" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('tags_index') }}">Retour à la liste</a>
        </li>
    </ul>
{% endblock %}
