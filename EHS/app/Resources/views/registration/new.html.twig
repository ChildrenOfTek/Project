{% extends 'base.html.twig' %}


{% block section %}

    <div class="row">
      <div class="col-md-10 col-md-offset-1">
      <div class="panel panel-default">
        <div class="panel-heading">S'inscrire à l'évènement {{ event.title }}</div>
        <div class="panel-body">
<div class="container">
{% include('events/showReg.html.twig') %}
    <h1>Registration creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('events_index') }}">Retour à la liste des évènements</a>
        </li>
    </ul>
</div>

{% endblock %}
